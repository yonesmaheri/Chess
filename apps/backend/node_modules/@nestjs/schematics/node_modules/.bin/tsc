#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../typescript/bin/tsc" "$@"
else
  exec node  "$basedir/../../../../typescript/bin/tsc" "$@"
fi
# cmd-shim-target=E:/Projects/chess.ir/Project/node_modules/.pnpm/@nestjs+schematics@11.1.0_c_9e4d713d3ac907a7527255c7b4c5d3ad/node_modules/typescript/bin/tsc
