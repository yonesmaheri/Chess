#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../@angular-devkit/schematics-cli/bin/schematics.js" "$@"
else
  exec node  "$basedir/../../../../@angular-devkit/schematics-cli/bin/schematics.js" "$@"
fi
# cmd-shim-target=E:/Projects/chess.ir/Project/node_modules/.pnpm/@nestjs+cli@11.0.23_@types+node@24.13.2_prettier@3.8.4/node_modules/@angular-devkit/schematics-cli/bin/schematics.js
