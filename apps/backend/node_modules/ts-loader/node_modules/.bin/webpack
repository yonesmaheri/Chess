#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../webpack/bin/webpack.js" "$@"
else
  exec node  "$basedir/../../../webpack/bin/webpack.js" "$@"
fi
# cmd-shim-target=E:/Projects/chess.ir/Project/node_modules/.pnpm/ts-loader@9.6.1_typescript@5.9.3_webpack@5.106.2/node_modules/webpack/bin/webpack.js
