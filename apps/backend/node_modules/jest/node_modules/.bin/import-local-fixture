#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../import-local/fixtures/cli.js" "$@"
else
  exec node  "$basedir/../../../import-local/fixtures/cli.js" "$@"
fi
# cmd-shim-target=E:/Projects/chess.ir/Project/node_modules/.pnpm/jest@30.4.2_@types+node@24._db12b2aa8704224a7dc94ca872ef6ec7/node_modules/import-local/fixtures/cli.js
