#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../jiti/lib/jiti-cli.mjs" "$@"
else
  exec node  "$basedir/../../../jiti/lib/jiti-cli.mjs" "$@"
fi
# cmd-shim-target=E:/Projects/chess.ir/Project/node_modules/.pnpm/eslint@9.39.4_jiti@2.7.0/node_modules/jiti/lib/jiti-cli.mjs
